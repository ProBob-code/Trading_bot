# GoatBotTrade Requirements
# ======================

# Core
flask>=3.0.0
flask-cors>=4.0.0
flask-socketio>=5.3.6
flask-login>=0.6.3
eventlet>=0.33.3
gunicorn>=21.2.0

# Data & Logic
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
python-dateutil>=2.8.2

# Logging & Utilities
loguru>=0.7.0
pyyaml>=6.0
python-dotenv>=1.0.0

# TA & ML
ta>=0.11.0
scikit-learn>=1.3.0
xgboost>=2.0.0

# TensorFlow (lazy-loaded by LSTM strategy only)
# Using tensorflow-cpu to avoid pulling CUDA deps on Railway
tensorflow-cpu>=2.13.0

# Yahoo Finance (for stock data)
yfinance>=0.2.30

# Database
mysql-connector-python>=8.0.33
pyotp>=2.9.0  # TOTP for Angel One SmartAPI login
websocket-client>=1.7.0  # Binance live price stream
